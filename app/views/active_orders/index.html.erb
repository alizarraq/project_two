 <%= render "shared/flash_messages" %>

<% if logged_in? &&  current_user.admin? %>

<h1 class="text-center mt-4">Listing all active orders</h1>

<div class="flickr_pagination">
  <%= will_paginate @all_active_orders, :container => false %>
</div>
<%= render 'all_active_orders' %>
<div class="flickr_pagination mb-4">
  <%= will_paginate @all_active_orders, :container => false %>
</div>
<%elsif logged_in? %>
<h1 class="text-center mt-4">Listing all active orders</h1>

<div class="flickr_pagination">
  
</div>
<%= render 'active_orders' %>
<div class="flickr_pagination mb-4">

</div>
<%else%>
  <h1 class="text-center mt-4  text-danger">You are not allowed to see all the orders</h1>
<%end%>

